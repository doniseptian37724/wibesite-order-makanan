{
  "name": "anti-gravity-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node anti-gravity/backend/src/server.js",
    "install": "cd anti-gravity/backend && npm install --production",
    "build": "echo 'No build step needed for frontend'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
